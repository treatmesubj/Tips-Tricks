rock@raspberrypi:~ $ docker stats  # show containers
rock@raspberrypi:~ $ docker exec -it pihole bash

root@pihole:/# tail -n 5000 /var/log/pihole.log | grep dhcp  # dhcp requests & offers
root@pihole:/# tail -n 100 /var/log/pihole-FTL.log | more
