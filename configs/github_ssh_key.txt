$ ls -al ~/.ssh
$ ssh-keygen -t my_key_for_github -C "jrock4503@hotmail.com"
$ ssh-add ~/.ssh/my_key_for_github
$ git config --global user.name "treatmesubj"
$ git config --global user.email "jrock4503@hotmail.com"

$ cat ~/.ssh/my_key_for_github.pub

# https://github.com/settings/keys
